[project]
name = "explainable-plagiarism-detector"
version = "0.13.0"
description = "A lightweight, explainable academic text-similarity research prototype."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
